# ムギミルチャンネル / リール企画室

このリポジトリは、ミニチュアダックス「ムギ」のInstagramアカウント
**ムギミルチャンネル** の運用ツール置き場です。

チャンネル名は **ムギ**(現在の愛犬)+ **ミル**(亡くなった先代犬ミルクの名前)から。
「ミル」はムギの別名ではないので、ムギを「ムギミル」と呼ばないこと。

- `reel-generator.html` … リール企画・台本ジェネレーター(ブラウザで動く本体)
- `docs/neta-bank.md` … ネタ帳(全企画の一覧)
- `docs/channel-context.md` … **チャンネルの設定・方針の1次資料**
- `scripts/video-sheet.sh` … 動画をコンタクトシート(コマ一覧)に変換するスクリプト
- `.claude/commands/` … スラッシュコマンド(`/neta` `/daihon` `/tensaku`)

## 作業前に読むもの

企画・台本・セリフ・キャプション・ハッシュタグに関わる依頼を受けたら、
まず `docs/channel-context.md` を読んでからその方針に沿って書くこと。

## 絶対に崩さないルール

- 画面に出る主役はムギ **1頭**。他の犬は登場させない。
- **ムギはオス・11歳。3歳のときの脊髄軟化症の手術で後ろ足が動かない(車椅子8年目)。**
  外では車椅子、家の中は後ろ足を引きずって前足で移動する。
  **2026年8月に膵炎を発症し、低脂質の食事しかとれない。**
  次を前提にした企画は、そのままでは撮れない:
  ①走る・ジャンプ・階段・飛んでくる ②ベッドに自分で上がる(補助が必要)
  ③高脂質のおやつを食べる ④水に入る・風呂を楽しむ(水が苦手)
  企画を出す前に `docs/channel-context.md` の「2-2. ムギの体のこと」を読むこと。
  悲劇として描かない。隠さない。日常として扱う。
- **食べものが出る企画は必ず低脂質のものだけ。** 迷ったら飼い主に確認し、推測で食材を決めない。
  「膵炎にはこれがいい」のような効能の断定はしない。経験の共有にとどめる。
- セリフはムギの心の声。**関西弁**で書く(一人称は「ボク」)。
  語尾は「〜やで / 〜やねん / 〜てん / 〜わ / 〜へん」。作り物の関西弁は使わない。
- 先代犬ミルクは追悼の対象。**ネタ・フック・オチの材料にしない**。
  こちらからミルクの企画を提案せず、飼い主が出したいときだけ静かなトーンで扱う。
  詳細は `docs/channel-context.md` の「2-1. 先代犬ミルクの扱い」に従う。
- 台本は4ブロック構成:冒頭テロップ(0〜2秒)→ 本編セリフ3〜5行 → オチ → CTA。
  (コールドオープン型のみ本編6行まで可)
- **CTAは1本に1つだけ**(フォロー / コメント / 保存のどれか)。表示は1.8秒以上。
- ハッシュタグは共通タグ(`BASE_TAGS` 6個)+ 企画固有タグ2個の計8個。羅列はしない。
- 不安を煽る健康情報や、断定的な獣医学的アドバイスは書かない。
- **人が映る回は顔を写さない(家族を含む。基本NG)。** 勤務中の人(配達員さんなど)は本人に一声かけてから投稿する。

## 企画を追加するとき

1. `reel-generator.html` の `IDEAS` 配列に既存と同じ書式で追加
   (`cat` / `title` / `hooks` / `lines` / `ochi` / `memo` / `tags`)
2. `cat` は `CATS` のキーから選ぶ
   (`toy` / `fashion` / `care` / `food` / `daily` / `voice` / `event`)
3. `docs/neta-bank.md` の一覧にも同じ企画を追記して番号を振る
4. 実績のある柱は **おもちゃ遊び / 服・おしゃれ / カット・お手入れ / 記念・ご報告**。
   新企画は「ビフォーアフター」「擬人化のギャップ」「節目の報告」のどれかに寄せる。
5. 構成は4ブロックのほかに3つの型がある。詳細は `docs/neta-bank.md` の末尾。
   - **ルーティン型**(No.22):時刻テロップで1日を追う。**最も再現性が高い**。
     時刻(事実)+ カッコ書きのツッコミ(感情)の2層構造。CTAはフォロー。本編は行数上限なし
   - **コールドオープン型**(No.23):いちばんいい画を冒頭2秒に先出しし、理由を最後に明かす
   - **レシピ型**(No.17):課題提起→材料◯つ→手順3つ→**保存CTA**。
     セリフは飼い主の声。**温度・時間は飼い主の実際の条件だけを使い、推測で足さない**

## 運用の決定事項

- **数値目標**:常時4桁再生(1,000以上)。1万超えを狙う。
  投稿後はこの基準で振り返る。4桁を割ったら冒頭2秒を疑う
- **場所**:「大阪府」までは出してよい。市区町村から下と、生活圏が特定できる
  店名・施設名は出さない(旅行先の宿や遠方の施設名は問題ない)
- **投稿**:リールは月・水・金 19:55。ストーリーズは毎日7〜8時 + リール投稿直後の20:00。
  水曜はトライアルリール(新規への検証枠)
- **「全力ぴょんぴょん」**はムギの代表的な動きの**正式名称**。毎回この呼び方で統一する
- **「かわいそう」系コメント**は議論しない。ムギの様子を伝えるだけ。毎回返さなくてよい。
  定型と対応の分け方は `docs/channel-context.md` の 8-1
- **PR・案件は受ける**。ただし「実際に使って良かったもの」「ムギの体に合うもの
  (低脂質・麻痺・シニア)」だけ。提供を受けた回は**キャプション冒頭と動画内テロップの
  両方に「PR」を明示**する(ステマ規制)。詳細は 8-2

## 動画を作るときの鉄則(トライアル2本で検証済み)

1. 冒頭1秒はムギの顔のアップ(**2本でいいねのピークを確認済み**)
2. テロップは横書き・**大きく**・1秒で読める字数(縦書きは使わない)
   トライアル(新規)は45歳以上が37%、**通常リール(既存)は67%**。
   通常リールはさらに大きく。字数を削ってでも文字を大きくする
   CTAも使い分ける:通常リール=いいね/コメント、トライアル=保存
3. 関西弁で統一
4. ストーリー構造(期待 → 異変 → 落胆 / 期待 → ハズレ → 本命)
5. **暗転のアウトロを作らない。12秒前後で切る**
6. 最後のテロップは明るいまま残す

根拠の数字は `docs/channel-context.md` の「4. コンテンツの柱」にある。

## 動画を見るとき

動画ファイルを渡されたら、**必ず `scripts/video-sheet.sh <動画>` を実行し、
出力された3枚のシートを Read ツールで画像として見ること**。
動画は直接再生できないので、コマを見ずに内容を推測してはいけない。
音声は聞けないため、音に関することは「確認してください」と飼い主に渡す。

## コードの注意

- `reel-generator.html` は依存ゼロの単一HTMLファイル。ビルド手順もサーバーも足さない。
- ライト/ダーク両テーマのCSS変数が定義済み。色を足すときは `:root` のトークンを使う。
- 日本語UIを保つ。文言を英語に置き換えない。
