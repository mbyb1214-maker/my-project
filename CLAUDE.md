# ムギミルチャンネル / リール企画室

このリポジトリは、ミニチュアダックス「ムギミル」のInstagramアカウント
**ムギミルチャンネル** の運用ツール置き場です。

- `reel-generator.html` … リール企画・台本ジェネレーター(ブラウザで動く本体)
- `docs/neta-bank.md` … ネタ帳(全企画の一覧)
- `docs/channel-context.md` … **チャンネルの設定・方針の1次資料**

## 作業前に読むもの

企画・台本・セリフ・キャプション・ハッシュタグに関わる依頼を受けたら、
まず `docs/channel-context.md` を読んでからその方針に沿って書くこと。

## 絶対に崩さないルール

- 主役はムギミル **1頭**。他の犬は登場させない。
- セリフはムギミルの心の声。一人称は必ず **「ボク」**。
- 台本は4ブロック構成:冒頭テロップ(0〜2秒)→ 本編セリフ3〜5行 → オチ → CTA。
- **CTAは1本に1つだけ**(フォロー / コメント / 保存のどれか)。
- ハッシュタグは共通タグ(`BASE_TAGS`)+ 企画固有タグ2個。羅列はしない。
- 不安を煽る健康情報や、断定的な獣医学的アドバイスは書かない。

## 企画を追加するとき

1. `reel-generator.html` の `IDEAS` 配列に既存と同じ書式で追加
   (`cat` / `title` / `hooks` / `lines` / `ochi` / `memo` / `tags`)
2. `cat` は `CATS` のキーから選ぶ
   (`toy` / `fashion` / `care` / `food` / `daily` / `voice` / `event`)
3. `docs/neta-bank.md` の一覧にも同じ企画を追記して番号を振る
4. 実績のある柱は **おもちゃ遊び / 服・おしゃれ / カット・お手入れ / 記念・ご報告**。
   新企画は「ビフォーアフター」「擬人化のギャップ」「節目の報告」のどれかに寄せる。

## コードの注意

- `reel-generator.html` は依存ゼロの単一HTMLファイル。ビルド手順もサーバーも足さない。
- ライト/ダーク両テーマのCSS変数が定義済み。色を足すときは `:root` のトークンを使う。
- 日本語UIを保つ。文言を英語に置き換えない。
